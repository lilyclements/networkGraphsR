% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_hierarchy_network.R
\name{plot_hierarchy_network}
\alias{plot_hierarchy_network}
\title{Plot a Hierarchical Network}
\usage{
plot_hierarchy_network(
  data,
  id_col = "__id",
  parent_col = "parent_id",
  child_col = "child_id",
  group_col = "group",
  ...
)
}
\arguments{
\item{data}{A data frame containing identifiers and relationship columns.}

\item{id_col}{The column in \code{data} containing unique identifiers for each node.
Defaults to \code{"__id"}.}

\item{parent_col}{The column in \code{data} specifying each item's parent identifier.
Defaults to \code{"parent_id"}.}

\item{child_col}{The column in \code{data} specifying each item's child identifier.
Defaults to \code{"child_id"}.}

\item{group_col}{A column used to group or color nodes in the network
(e.g., by responsible person, category, or type). Defaults to \code{"group"}.}

\item{...}{Additional arguments passed to \code{networkD3::forceNetwork()},
such as \code{legend}, \code{bounded}, or \code{fontSize}.}
}
\value{
An interactive \code{htmlwidget} object produced by \code{networkD3::forceNetwork()},
which can be viewed directly in RStudio, a browser, or embedded in
R Markdown or Shiny applications.
}
\description{
Creates an interactive network visualization of hierarchical relationships
(e.g., parent–child links) within a dataset using
\strong{\{networkD3\}}'s \code{forceNetwork()} function.

The function identifies parent–child connections, constructs node and link
indices, and plots a force-directed network where each node represents an
entity (such as an initiative or activity), and links show hierarchical
relationships.
}
\details{
This function first extracts parent–child pairs from the specified columns,
builds a list of all unique nodes, assigns each a numeric index, and then
joins those indices back to create the link table required by
\code{networkD3::forceNetwork()}.

Each node is associated with a group label (if provided), which controls
its color in the resulting visualization.
}
\examples{
\dontrun{
# Example dataset
df <- data.frame(
  id = c("A", "B", "C", "D"),
  parent_id = c(NA, "A", "A", "B"),
  child_id = c("B", "C", "D", NA),
  group = c("Team 1", "Team 1", "Team 2", "Team 2")
)

# Plot the hierarchy network
plot_hierarchy_network(
  data = df,
  id_col = "id",
  parent_col = "parent_id",
  child_col = "child_id",
  group_col = "group",
  legend = TRUE,
  fontSize = 16
)
}

}
\seealso{
\code{\link[networkD3:forceNetwork]{networkD3::forceNetwork()}}
}
